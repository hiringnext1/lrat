# GrowLeadz — robots.txt
# https://growleadz.co

# Allow all search engines and AI crawlers full access to public pages
User-agent: *
Allow: /

# Block backend API routes from indexing
Disallow: /api/

# Block internal app pages from indexing
Disallow: /dashboard/
Disallow: /onboarding/

# NOTE: /login and /signup are intentionally crawlable — they carry a noindex
# meta tag, which crawlers can only act on if they are allowed to fetch the page.

# Sitemap location
Sitemap: https://growleadz.co/sitemap.xml
